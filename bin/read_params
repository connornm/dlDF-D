#!/bin/bash
# Gets parameters from NAME.dat file
# Reads from either the initial parameters directory: init-param
# Also can read from parameter directory: param
# Usage:
#	./read_params DIR/NAME

name=${1}
cat data/${name}'.dat'
